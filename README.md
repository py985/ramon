# ramon

Gerado automaticamente pelo Gerador de App.

## Como gerar o instalador (Windows)
- Execute o arquivo `build.bat`

## Como gerar o instalador (Mac/Linux)
- Execute o arquivo `build.sh`

O instalador estará na pasta `out/make/`